<div class="container">
  <h1>Flats</h1>
  <ul>
    <% @flats.each do |flat| %>
      <li>
        <%= link_to flat_path(flat["id"]) do %>
          <h2><%= flat["name"] %></h2>
        <% end %>
        <p>💰 <%= flat["price"] %> <%= flat["priceCurrency"] %></p>
      </li>
    <% end %>
  </ul>
</div>
