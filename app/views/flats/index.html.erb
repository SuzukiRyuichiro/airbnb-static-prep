<div class="container">
  <h1>Flats</h1>
  <ul>
    <% @flats.each do |flat| %>
      <li>
        <h2><%= flat["name"] %></h2>
        <p>💰 <%= flat["price"] %> <%= flat["priceCurrency"] %></p>
      </li>
    <% end %>
  </ul>
</div>
